<%- include('layout/navbar') %>
<h1>Songs</h1>

<% songs.forEach((song) => { %>

<div class = "row">
  <h2><%= song.title %></h2>
  <p>Artist</p>
  <p>Album</p>
  <% if(song.duration%60 < 10){ %>
    <h3><%= Math.floor(song.duration / 60) %>:0<%=song.duration % 60%></h3>
  <% } else{%>
    <h3><%= Math.floor(song.duration / 60) %>:<%=song.duration % 60%></h3>
  <% } %>
</div>
<% }) %>
